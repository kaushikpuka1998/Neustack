spring.application.name=Neustack
server.port=8081
# Custom path for the raw OpenAPI json specification (Default is /v3/api-docs)
springdoc.api-docs.path=/api-docs

# Custom path for the interactive UI browser (Default is /swagger-ui.html)
springdoc.swagger-ui.path=/swagger-ui.html
